node_modules/
dist/
*.log
.scratch/
.context/

# eval data is private (your own conversation history); only code is committed.
# Anything under eval/private/ stays local — fail-safe by default.
/eval/private/

